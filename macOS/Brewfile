cask_args appdir: '/Applications'

# Taps: none needed — homebrew/core and homebrew/cask are auto-tapped by modern
# Homebrew, and the old cask-fonts / cask-versions taps were merged into cask.

# --- CLI tools ---
brew "asdf"                      # version manager (ruby, nodejs, ...) — replaces rbenv
brew "awscli"
brew "bat"                       # `cat` alias in .aliases
brew "coreutils"
brew "curl"
brew "fzf"
brew "gemini-cli"
brew "gh"
brew "htop"
brew "jq"
brew "libpq"                     # psql client (added to PATH in .zshrc)
brew "libyaml"
brew "neovim"
brew "pinentry-mac"              # GPG pinentry (GPG_TTY set in .zshrc)
brew "ripgrep"                   # neovim: fzf-lua live grep (\)
brew "starship"                  # available prompt (not enabled by default)
brew "tmux"
brew "tree"
brew "tree-sitter-cli"           # neovim: nvim-treesitter (main) builds parsers
brew "zsh"
brew "zsh-autosuggestions"
brew "zsh-syntax-highlighting"

# --- Casks ---
cask "claude-code"
cask "codex"
cask "copilot-cli"
cask "font-fira-code"
cask "font-fira-code-nerd-font"
cask "font-hack-nerd-font"
cask "font-meslo-lg-nerd-font"
cask "warp"
